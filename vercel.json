{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "UV_LINK_MODE=copy uv pip install --system -r requirements-server.txt && python build.py && npm run build",
  "outputDirectory": "website/client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ],
  "functions": {
    "api/index.py": {
      "includeFiles": "data.json",
      "excludeFiles": "data/storage.db"
    }
  },
  "env": {
    "CHESS_REPO_TYPE": "json",
    "CHESS_JSON_PATH": "data.json",
    "UV_LINK_MODE": "copy"
  }
}
